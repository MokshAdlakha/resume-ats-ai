# resume-ats-ai
Flask web app that parses multiple resumes (PDF/DOCX), extracts candidate info using spaCy &amp; regex, matches skills against a job description, and scores/ranks applicants automatically.
